# 学習済みルール

## 2026-06-14 「線を長くして」の対象取り違え／大圏線×メルカトルの構造限界を見落とした

### 何を間違えたか
/direction-map で「境界線が短い、長くして」と言われ、最初コンパス線（`lineDist`）を変更したが、実際の対象は地図上の扇形カラーゾーンの距離（`sectorDist`）だった。さらに距離を 15,000km に伸ばしたら、大圏線がメルカトル地図で日付変更線（経度±180°）を越え、画面を端から端まで横切る横線2本が出た。

### 再発防止ルール
- 地図UIで「この線を長く」と言われたら、まず**実機スクショで対象の線（扇形ゾーン／コンパス線／放射線）を特定**してから値を変える。コードの変数名だけで推測しない
- **大圏線×メルカトルで起点から長距離の線・面を描くと日付変更線越えで描画が破綻する**。距離を伸ばす方式には構造的上限がある（東西方向は約4,000〜5,000kmで日付変更線到達）。画面端まで伸ばすには距離指定でなく「地図表示境界までクリップ＋経度ラップ処理」が必要
- 扇形の「外周線（外端どうしを結ぶ弦）」を消すには、`L.polygon` を `stroke:false`（塗りのみ）にし、境界線は `L.polyline` で別途描く。polygon の輪郭で閉じると弦＝横線が出る

### 2026-06-14 追加：扇形塗りの横線は「塗り自体が極を越える」のが原因。境界線polylineを短くしても消えない

最初の対処で「境界線polylineだけ短くして塗りはそのまま」とやったが、横線は消えなかった。横線（弦）は **L.polygon の塗り範囲が北極を越えた瞬間、メルカトルで経度反転して反対側に塗りが広がる**ことで発生する。塗り自体（sectorDist）を極の手前で打ち切らないと意味がない。次回類似ケースの判別：

- 境界線（polyline・線）が極を越える → 線として横切る（細い線）
- 塗り（polygon・面）が極を越える → 帯として横切る（広い帯・色付き）
- どちらにせよ「距離方式」を使う限り北極到達距離（東京から約6,100km）が物理上限

### Codex 関連の学び
- Codex はブリーフに「page.tsx は中央地図そのまま／右カラム触るな」と明記しても **page.tsx を全置換**して地図・右カラムを消した。複雑な既存UIの部分差し替えは Claude 直編集の方が安全な場合がある（`git checkout HEAD -- <file>` で復旧した）
- ブリーフ起動は PowerShell 5.1 で引数渡しすると本文中の `flex gap-1` 等がフラグ誤認される。**stdin 経由（`Get-Content ... -Raw | codex exec ... -`）**で渡す